# Test Report

Executed after the second-round backend changes: `scripts/test-all.ps1` -> 15 passed; `flutter test` -> 5 passed; `flutter analyze` -> 0 issues. The API flow additionally verifies static repository indexing, DNS/private-range rejection, memory ownership/search/delete, Agent schema validation, structured/provider tool execution, tool audit rows, and a 20-query retrieval evaluation baseline.

Fresh Android verification: `scripts/build-all.ps1` -> PASS, producing `artifacts/android/knowflow-ai-release.apk` (21,323,381 bytes, SHA256 `F47A0D6B19B2A6014F89F3C3EAA1607A290DDD1E0579C4B46DA885B121DB5E59`). `apksigner verify --verbose` confirms a single v1/v2 signer; local release certificate SHA-256 is `4A:21:F2:17:0C:FD:FB:40:0A:D7:CE:67:3D:63:01:94:D8:B0:3F:F2:28:53:7F:8C:D3:AD:4B:50:A3:80:3D:2D`. Prior Android emulator evidence covers fresh install and TXT/MD/PDF/DOCX/PPTX upload, delete, picker cancel, offline failure and recovery. The current shell has no `adb.exe` on PATH, so this turn did not rerun UI clicks after the backend-only changes.

HarmonyOS target configuration remains `6.0.2(22)`. `scripts/build-all.ps1` -> PASS, producing `artifacts/ohos/knowflow-ai-release.hap` (20,618,191 bytes, SHA256 `D12EEF03B97ABCB4B2E1772C8398B4432418B23CFCB591BFE8C5A71889312E8C`) with local x86_64 signing. The available `hdc` targets remain `COM3/COM4 UART Ready` without a guest emulator, so HarmonyOS runtime/picker/ARM64 checks are `BLOCKED` or `UNVERIFIED`.
