# RAG Eval

The deterministic fixture is `sample_data/rag_notes.md`. The API test covers retrieval and citation presence. A 20-question corpus can be added when a production embedding/rerank service is configured; this local build intentionally reports only the executed checks and does not invent recall metrics.
