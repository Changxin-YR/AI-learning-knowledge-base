2026-09-07
Toolchain: CPF-Flutter 3.27.4 / DevEco SDK HarmonyOS 6.1.1 API 24 / target and compatible SDK 6.0.2(22) / hvigorw / ohpm 6.1.2.285
Command: `hvigorw assembleHap --no-daemon`
Result: PASS. Hvigor produced `mobile/ohos/entry/build/default/outputs/default/entry-default-unsigned.hap`, copied to `artifacts/ohos/knowflow-ai-release-unsigned.hap`. The previous `llvm-strip.exe` warning is resolved by using the DevEco SDK path in `mobile/ohos/local.properties`.
Command: `flutter build hap --release --target-platform ohos-x64 --dart-define=API_BASE_URL=http://10.0.2.2:8001`
Result: PASS. The signed HAP was produced at `artifacts/ohos/knowflow-ai-release.hap` with `libs/x86_64/libapp.so` and `libs/x86_64/libflutter.so` for the emulator ABI.
Device check: `hdc list targets` -> `127.0.0.1:5555` (HarmonyOS 6.0.2/API 22 emulator, x86_64). The HAP was installed after removing the previous package with a different debug signature, then launched successfully.
Runtime evidence: home loaded backend data (`已索引 10 份文档`); AI question `RAG` returned a deterministic answer and displayed `来源：notes.md`. Screenshots/layout dumps: `artifacts/ohos/real-online-home-rebuilt.png`, `artifacts/ohos/real-online-ai-rebuilt.png`, `artifacts/ohos/real-online-ai-rebuilt-input.xml`, `artifacts/ohos/real-online-ai-response-rebuilt.png`, and matching XML files.
Known limits: no physical device tested; no real LLM provider tested; landscape, split/floating windows, dark mode, and AppGallery performance thresholds remain unverified.
Signing note: the debug certificate and keystore passwords used for this local run remain outside the public repository.
